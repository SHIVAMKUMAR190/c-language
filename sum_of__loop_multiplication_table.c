#include <stdio.h>
int main()
{
    int i = 0;
    int n;
    int sum = sum + n * i;

    printf("enter no=");
    scanf("%d", &n);

    for (i = 1; i <= 10; i++)

    {
        printf("table:%d\n", n * i);
        sum = sum + n * i;
    }
    printf("total no%d\n", sum);

    return 0;
}